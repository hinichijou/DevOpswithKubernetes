apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: todo-app
  namespace: argocd
spec:
  goTemplate: true
  generators:
  - list:
      elements:
      - name: todoapp-production
        path: todo_app/overlays/prod
      - name: todoapp-staging
        path: todo_app/overlays/staging
  template:
    metadata:
      name: '{{.name}}'
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: https://github.com/hinichijou/DevOpswithKubernetes.git
        targetRevision: HEAD
        path: '{{.path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
