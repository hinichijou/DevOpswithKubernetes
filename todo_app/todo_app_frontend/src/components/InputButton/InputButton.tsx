import './InputButton.css'

const InputButton = () => {
    return <input
        name="todo_submit"
        type="submit"
        className="button"
        value="Send"
    />
}

export default InputButton
