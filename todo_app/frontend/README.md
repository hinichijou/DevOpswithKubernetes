## Todo app frontend

Todo app React + Vite frontend project.

Displays a random image fetched by backend and a list of todos.

The frontend project can be tested locally by running:

`npm install`

`npm run dev`

Default port is 5173 and application can be accessed from http://localhost:5173.