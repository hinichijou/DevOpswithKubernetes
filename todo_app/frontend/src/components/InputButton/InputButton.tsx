import './InputButton.css'

const InputButton = () => (
    <input
        name="todo_submit"
        type="button"
        className="button"
        value="Send"
    />
)

export default InputButton
