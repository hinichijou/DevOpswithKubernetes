apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: project
configMapGenerator:
- name: todos-data
  files:
  - ./todos.json
- name: db-init
  files:
  - ./init_todo_db.sql
resources:
  - configmap_todoapp.yaml
  - deployment.yaml
  - ingress.yaml
  - service.yaml