apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: project-production
# Seems to cause potential issues
# namePrefix: prod-
resources:
  - manifests/sealed_secret_postgres_production.yaml
  - ./../../base
patches:
  - path: manifests/persistentvolumeclaim_todoapp_prod.yaml