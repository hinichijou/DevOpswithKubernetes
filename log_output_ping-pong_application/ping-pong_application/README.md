## Ping-pong application

Node.js express server application. Counts how many times http://localhost:*insert_defined_port_here*/pingpong is requested and outputs the count. Writes the count to a file called `pingpongs.txt` after incrementing the counter.

Can be run locally with `npm run dev`. With default port available at http://localhost:3000/pingpong.